{
  "name": "channel-backup-template",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "sequence:router": "channel-backup --config ./router/config.json"
  },
  "dependencies": {
    "channel-backup": "^1.2.5"
  }
}
